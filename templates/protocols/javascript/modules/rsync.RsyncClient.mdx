# Class: RsyncClient

[rsync](/templates/protocols/javascript/modules/rsync).RsyncClient

RsyncClient is a client for RSYNC servers.
Internally client uses https://github.com/gokrazy/rsync driver.

**`Example`**

```javascript
const rsync = require('nuclei/rsync');
const client = new rsync.RsyncClient();
```

## Table of contents

### Constructors

- [constructor](/templates/protocols/javascript/modules/rsync.RsyncClient#constructor)

### Methods

- [Connect](/templates/protocols/javascript/modules/rsync.RsyncClient#connect)
- [ListFilesInModule](/templates/protocols/javascript/modules/rsync.RsyncClient#listfilesinmodule)
- [ListModules](/templates/protocols/javascript/modules/rsync.RsyncClient#listmodules)

## Constructors

### constructor

• **new RsyncClient**(): [`RsyncClient`](/templates/protocols/javascript/modules/rsync.RsyncClient)

#### Returns

[`RsyncClient`](/templates/protocols/javascript/modules/rsync.RsyncClient)

#### Defined in

rsync.ts:28

## Methods

### Connect

▸ **Connect**(`host`, `port`, `username`, `password`, `module`): `boolean`

Connect establishes a connection to the rsync server with authentication.

#### Parameters

| Name | Type |
| :------ | :------ |
| `host` | `string` |
| `port` | `number` |
| `username` | `string` |
| `password` | `string` |
| `module` | `string` |

#### Returns

`boolean`

**`Example`**

```javascript
const rsync = require('nuclei/rsync');
const client = new rsync.RsyncClient();
const connected = client.Connect('acme.com', 873, 'username', 'password', 'backup');
```

#### Defined in

rsync.ts:39

___

### ListFilesInModule

▸ **ListFilesInModule**(`host`, `port`, `username`, `password`, `module`): `string`[]

ListFilesInModule lists files in a specific module on the rsync server.

#### Parameters

| Name | Type |
| :------ | :------ |
| `host` | `string` |
| `port` | `number` |
| `username` | `string` |
| `password` | `string` |
| `module` | `string` |

#### Returns

`string`[]

**`Example`**

```javascript
const rsync = require('nuclei/rsync');
const client = new rsync.RsyncClient();
const files = client.ListFilesInModule('acme.com', 873, 'username', 'password', 'backup');
log(toJSON(files));
```

#### Defined in

rsync.ts:67

___

### ListModules

▸ **ListModules**(`host`, `port`, `username`, `password`): `string`[]

ListModules lists available modules on the rsync server.

#### Parameters

| Name | Type |
| :------ | :------ |
| `host` | `string` |
| `port` | `number` |
| `username` | `string` |
| `password` | `string` |

#### Returns

`string`[]

**`Example`**

```javascript
const rsync = require('nuclei/rsync');
const client = new rsync.RsyncClient();
const modules = client.ListModules('acme.com', 873, 'username', 'password');
log(toJSON(modules));
```

#### Defined in

rsync.ts:53
